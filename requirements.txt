# === 核心依赖（必须安装）===
mediapipe>=0.10.0        # Pose/Holistic 人体关键点检测（仅支持Python 3.9-3.11）
opencv-python>=4.8.0     # 摄像头采集、图像处理
numpy>=1.24.0            # 数值计算
plotly>=5.15.0           # 交互式3D可视化 + 气泡标注（浏览器）
pillow>=10.0.0           # 中文文字渲染到图像（穴位名称标注）

# === 可选依赖 ===
# pyyaml>=6.0           # 如需从config.yaml动态加载配置请添加
# open3d>=0.17.0        # 如需桌面原生3D窗口请添加
# gradio>=3.50.0        # 如需Web UI界面请添加
# scipy>=1.10.0         # 如需高级插值算法请添加

